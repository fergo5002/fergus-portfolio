# F0 sweep, 2026-09-03

Run before the repository went public, per `docs/superpowers/plans/2026-09-03-toolshed-f0-ship-path.md`, Task 1.

- gitleaks (`zricethezav/gitleaks:latest`, image digest `c00b6bd0`) over `--log-opts="--all"`: 82 commits, 1.90 MB scanned, **1 finding**: `generic-api-key` in `scripts/indexnow.mjs` line 24, commit `cb3e60a`. That is the IndexNow key, which the protocol requires to be public: it is served at the site root as `public/1e1c07d6835b43b5ae97096bb927a1ee.txt` and the script's own header says it is committed on purpose. Not a secret.
- Shape grep over the full history (Resend `re_`, PostHog `phc_`, GitHub `ghp_`, Stripe `sk_`, AWS `AKIA`, PEM headers): no matches.
- Sensitive filenames in history (`.env*`, `.pem`, `.key`, `credentials`, `secret`): only `content/articles/one-webhook-secret-two-tenants.ts`, which is an article about webhook secrets and contains none.
- Hand pass over `content/`, `public/`, `app/`, `lib/`, `components/`: every hit is prose, an env var name, or the rate limiter's `tokens` counter. `public/` holds the IndexNow key file, the Google Search Console verification file, `data/` and `img/`. Both proof files stay.
- Remote branches `origin/feat/phosphor-motion-system` and `origin/feat/real-imagery` were fetched locally and so were inside `--all`.
- Verdict: **clean**.
- Not checked: pull-request refs that were never fetched (`refs/pull/*`), and any branch deleted on the remote before today. The local `.env.local` holds only the public PostHog write key and is gitignored.
